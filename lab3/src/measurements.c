#include <stdlib.h>

int main(int argc, char* argv[]) {/*
    FILE* out = fopen("data/measurements.txt");
    if (!out) { return 1; }

    int N;
    printf("Podaj liczbe pomiarow: "); scanf("%d", &N)

    double* pomiary;
    pomiary = (double*)malloc(N*sizeof(double));

    for (double* curPomiar = pomiary; curPomiat <= sizeof(double) * N; curPomiar++) {
        scanf("%lf", *curPomiar);
        fprintf("%lf\n", *curPomiar);
    }


    

    
    
    fclose(out);
    free(pomiary);
    */
}